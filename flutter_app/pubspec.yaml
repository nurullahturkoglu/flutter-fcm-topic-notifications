name: flutter_fcm_topic_notifications
description: A Flutter app for FCM topic and token-based push notifications
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^4.1.0
  firebase_messaging: ^16.0.4
  flutter_local_notifications: ^19.5.0
  dio: ^5.4.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
